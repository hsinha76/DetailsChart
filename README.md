# DetailsChart

A simple app with following features:

* Mobile OTP login
* Fetches data from API and displays it in bar graph form.
* Save the response in local database, so if Network is not available the next time the graph should still be visible.
